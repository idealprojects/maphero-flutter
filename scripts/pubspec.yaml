name: maphero_code_gen
description: code generation for flutter-mapbox-gl
publish_to: 'none'

version: 0.0.7

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  mustache_template: ^2.0.0
  recase: ^4.0.0

dev_dependencies:
  very_good_analysis: ^7.0.0